# ---------- Docker (frontend) ----------
# Puerto público donde escucha Nginx (80, o 8080 si delante tienes otro proxy con HTTPS)
WEB_PORT=80
# Dirección de la API. Por defecto, el contenedor del backend en la red jamuywasi_net
API_UPSTREAM=http://backend:8000
